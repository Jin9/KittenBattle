using UnityEngine;
using UnityEngine.UI;
using System.Collections;

public class setsubmenu : MonoBehaviour {

	public Canvas c;
	// Use this for initialization
	void Awake () {
		c.gameObject.SetActive (false);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
